@model List<MedicineInventoryViewModel>
@{
    ViewData["Title"] = "Expired Medicines";
}
<h2 class="text-danger text-center">❌ Expired Medicines</h2>
<a class="btn btn-outline-secondary mb-3" href="/Pharmacy/Index">Back to Inventory</a>

@if (!Model.Any())
{
    <div class="alert alert-danger">No expired medicines found.</div>
}
else
{
    <table class="table table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Name</th>
                <th>Quantity In Stock</th>
                <th>Expiry Date</th>
                <th>Low Stock Threshold</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var med in Model)
            {
                <tr class="table-danger">
                    <td>@med.MedicineName</td>
                    <td>@med.QuantityInStock</td>
                    <td>@med.ExpiryDate.ToShortDateString()</td>
                    <td>@med.LowStockThreshold</td>
                    <td>
                        <a href="/Pharmacy/Edit/@med.Id" class="btn btn-sm btn-danger">Review</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
